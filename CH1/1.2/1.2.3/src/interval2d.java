public class interval2d {
    public static void main(String[] args) {
        /*
        int N = Integer.parseInt(args[0]);
        double min = Double.parseDouble(args[1]);
        double max = Double.parseDouble(args[2]);
        StdDraw.setPenRadius(0.01);
        StdDraw.setPenColor(StdDraw.ORANGE);
        StdDraw.setXscale(-10, 10);
        StdDraw.setYscale(-10, 10);
        StdDraw.square(min, max, (max - min) / 2);

        Interval2D i2g[] = new Interval2D[N];
        for (int i = 0; i < N; i++) {
            double left = StdRandom.uniform(min, max);
            double base = StdRandom.uniform(min, max);
            Interval1D width = new Interval1D(left, StdRandom.uniform(left, max));
            Interval1D high = new Interval1D(base, StdRandom.uniform(base, max));
            i2g[i] = new Interval2D(width, high);
            i2g[i].draw();
        }
*/

    }
}
